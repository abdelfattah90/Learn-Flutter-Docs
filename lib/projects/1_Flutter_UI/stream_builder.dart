import 'package:flutter/material.dart';

import '../../widgets.dart';

class StreamBuilderPage extends StatelessWidget {
  const StreamBuilderPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: appbarWidget('StreamBuilder'),
      body: Container(),
    );
  }
}
