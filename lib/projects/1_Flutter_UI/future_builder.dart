import 'package:flutter/material.dart';
import '../../widgets.dart';

class FutureBuilderPage extends StatelessWidget {
  const FutureBuilderPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: appbarWidget('FutureBuilder'),
      body: Container(),
    );
  }
}